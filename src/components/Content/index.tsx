import { ContentHeader, ContentDescription, ContentContainer, ContentBody } from './styles';

export const Content = {
	Header: ContentHeader,
	Description: ContentDescription,
	Container: ContentContainer,
	Body: ContentBody,
};
